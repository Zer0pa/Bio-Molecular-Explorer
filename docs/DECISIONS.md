# Architectural Decisions Log

Research use only. Not for diagnosis, treatment, cure claims, prescribing, clinical deployment, regulatory compliance, or drug-safety certification.

<!-- APPEND-ONLY. Future agents: add new rows at the bottom. NEVER edit existing rows. -->

---

| ID | Date | Decision | Rationale | Reversibility | Impact |
|----|------|----------|-----------|---------------|--------|
| D-001 | 2026-04-30 | Python-first stack: FastAPI + Pydantic v2 + pytest + pyyaml + jsonschema + httpx + typer + networkx | Every adapter shares the same envelope/contract import path; eliminates Python/TypeScript split complexity. Addressed PRD §12 Q#2. | Reversible: TypeScript layer can be added without changing Python contracts. | Entire codebase; base architecture decision. |
| D-002 | 2026-04-30 | `src/zer0pa_biomolecular_explorer/` package layout: contracts/, layers/{l1..l6}/, audit/, kg/, falsifiers/, packets/, reasoner/, cloud_lab/, orchestration/ | Clean separation of concerns; each subdirectory is a coherent domain. Addressed PRD §12 Q#1. | Reversible: directories can be split into separate packages without breaking imports. | All import paths. |
| D-003 | 2026-04-30 | No RDKit on originating Mac; SMILES validation is regex-based | Avoids heavy native dependency on the CPU build. RDKit is an optional `[chem]` extra for Runpod. Falsification wave does not require real SMILES canonicalization. | Reversible: `pip install -e .[chem]` adds RDKit. | L1 adapter, compound fixtures (literal SMILES accepted as-is). |
| D-004 | 2026-04-30 | JSON Schema mirror approach: canonical contract is JSON Schema; Pydantic models validate against it | Dual-layer validation: machine-readable schema (for external tools) + Pydantic (for runtime). Addressed schema-Pydantic alignment surfaced during reasoner subagent review. | Reversible: JSON Schema can be dropped if Pydantic schema export suffices. | envelope.py, reasoner/tuple, compound fixtures. |
| D-005 | 2026-04-30 | Clinical-overclaim sanitization: sha256-prefix-hashing detected phrases in falsifier evidence | Storing banned phrases verbatim in audit records re-trips the validator on every read. Hash prefix preserves traceability without leaking the phrase. Surfaced by falsification wave. | Irreversible as a security invariant; the leak would be an audit contamination. | `detect_clinical_overclaim()`, `AuditValidator._check_record_shape()`. |
| D-006 | 2026-04-30 | Cardiac balance score sign: outward_block(IKr+IKs) - inward_block(INaL+ICaL) | Higher = more outward block = APD-prolongation indicator. Verapamil score is lower than dofetilide due to ICaL compensation — the canonical hERG-only-overreach refuter. Documented in assembler and packet multi_current_interpretation. | Reversible: sign convention is a display choice. Any change requires updating all packet claims. | packets/assembler.py, L5 bridge, multi_current_interpretation text. |
| D-007 | 2026-04-30 | Verapamil SCN5A explicit_absence flag added retroactively | hERG-only-overreach detector failed because null ic50_uM without explicit_absence was ambiguous. Adding explicit_absence makes the data quality gap visible and auditable. Pattern generalized to all fixtures. | Reversible: remove flag when real SCN5A assay data arrives. | verapamil.json, compound.schema.json (explicit_absence required on null channels). |
| D-008 | 2026-04-30 | AuditWriter.append() stamps schema_version + created_at_utc + research_boundary if absent | Callers cannot accidentally produce records missing required fields. Hash chain computed over the fully-stamped payload. | Reversible: callers can pre-supply these fields; the stamp is idempotent. | AuditWriter, all audit record writers. |
| D-009 | 2026-04-30 | L4 does NOT include COPASI/Tellurium (belongs in L5) | PRD brief correction. Enforced by code (no imports) and tests. L4 is FMU bus + sensor simulation; L5 is PKPD + SBML. | Reversible: if L4/L5 boundary shifts, update layer contracts and tests. | L4 adapter, layer contract definitions. |
| D-010 | 2026-04-30 | OpenFE Runpod adapter: constructs without raising; only process() raises RuntimeError | Proves cutover is a backend flag flip, not an architectural rewrite. Satisfies PRD §2 plug-replaceability invariant. The error is self-describing. | N/A: the stub-vs-real distinction is the mechanism. | L1 adapter, plug-replaceability tests. |
| D-011 | 2026-04-30 | Six held-out benchmark compounds chosen: quinidine, moxifloxacin, diltiazem, sotalol, mexiletine, lidocaine | Cover multi-channel (quinidine), QT-only IKr reference (moxifloxacin, sotalol), ICaL reference (diltiazem), INa/INaL references (mexiletine, lidocaine). Collectively exercise all four channel dimensions. | Reversible: fixtures can be swapped; test count asserts exactly 6. | fixtures/compounds/{quinidine..lidocaine}.json, test_held_out_packets.py. |
| D-012 | 2026-04-30 | compound.schema.json uses JSON Schema 2020-12 draft; panel entry additionalProperties:false with optional explicit_absence/source_refs/note allowed | Strict schema prevents undocumented fields; optional fields are whitelisted. Consistent with envelope schema approach (D-004). | Reversible: add more optional fields to the whitelist as needed. | schemas/fixtures/compound.schema.json, all compound fixtures. |
| D-013 | 2026-04-30 | Stub SMILES for held-out fixtures are literature values (PubChem), documented in stub_provenance_note | RDKit not available for canonical normalization (D-003). Literature SMILES are the best available CPU-side value. All must be replaced at Runpod cutover. | Reversible: replace with RDKit-canonical SMILES at cutover. | fixtures/compounds/{quinidine..lidocaine}.json. |
| D-014 | 2026-04-30 | Cardiac end-to-end run writes ALL 12 audit tables per compound (runs, molecules, model_tools, source_manifest, parameters, confidence, falsifiers, decisions, artifacts, replay_commands, offload_manifest, midd_assessments). | PRD section 6 lists all 12 as required. Replay, MIDD, and offload manifest are now part of the run, not just declared in audit/__init__.py. | Reversible only via audit schema change. | runs/cardiac_run.py; tests/integration/test_cardiac_run.py. |
| D-015 | 2026-04-30 | L6Router silent_falsifier_loss check operates on FAIL-only items (PASS items dropping is informational, not a violation). | Forward layer chains legitimately scope different falsifiers per layer. Without this filter, the router blocked at L2.5 because L1's PASS items were dropped — a false positive. | Reversible. | orchestration/router.py; tests/integration/test_l6_router_run.py. |
| D-016 | 2026-04-30 | Two adapters per layer minimum: primary `*StubAdapter` + deliberately-different-valued `*ToyAdapter`. Plug-swap tests use Stub-as-A, Toy-as-B. | Two instances of one class do not prove plug-replaceability. The toy is a separate code path with different canned values. PRD section 2 invariant becomes truly testable. | Reversible (toys are pure additions). | layers/{l1..l5}/toy_adapter.py; tests/plug_swap/test_plug_replaceability.py. |
| D-017 | 2026-04-30 | `runpod-precheck` CLI command reads `runpod.config.yaml` and reports per-adapter blockers. CPU-stub state is the expected pass condition. | Cutover is a script, not just docs. The procedure is itself testable on a CPU-only build. | Trivially reversible. | cli.py; tests/integration/test_cardiac_run.py. |
| D-018 | 2026-04-30 | `kg/` runtime nodes/edges are append-only across runs. `kg/cardiac_seed.jsonl` stays as bootstrap; `kg/{nodes,edges}.jsonl` accumulate. | Episodic memory: each run adds to the lab's private corpus. The synthesis-agent's "falsification ledger as product surface" Inversion D. | Reversible (rm + reseed). | kg/store.py; runs/cardiac_run.py; CLI `validate-kg` checks across runs. |
| D-019 | 2026-04-30 | Runtime `Claim` nodes ship with synthetic `EvidenceItem`, `HAS_SOURCE`, `HAS_FALSIFIER`, `HAS_AUDIT` edges to satisfy K1. | A claim promoted to `supported_for_research` MUST be fully grounded; emitting only a Claim node correctly triggers K1 violation. We emit the supporting graph instead of lowering the status. | Reversible. | runs/cardiac_run.py. |
| D-020 | 2026-04-30 | Pathway 1 layers use semantic names (`P1.Target`, `P1.Structure`, `P1.Generate`, `P1.Screen`, `P1.Optimize`, `P1.Handoff`) instead of numeric (`P1.L1`, ...). | Numeric naming would collide with existing L1-L6 cardiac-safety semantics. Semantic eliminates ambiguity. | Reversible (rename with codemod). | LayerName enum; `src/zer0pa_biomolecular_explorer/pathway1/`. |
| D-021 | 2026-04-30 | New `Backend.EXTERNAL_API` value for tools that are remote API calls (GPT-Rosalind, Open Targets, TTD, GWAS Catalog, PubTator, ChEMBL, ZINC-22). | Neither stub nor GPU-bound — synchronous HTTPS with latency, cost, license terms. Existing enum doesn't capture this class. | Reversible. | envelope.py; runpod.config.yaml extension. |
| D-022 | 2026-04-30 | `P1HandoffPacket` carries optional `l1_channel_panel_input` block matching existing `L1ChannelPanelInput` shape. | Existing L1 cardiac contract is closed-enum; Pathway 1 generates general targets. The packet bridges: cardiac targets get the L1 panel populated; non-cardiac get null. | Reversible. | pathway1/contracts/p1_handoff.py. |
| D-023 | 2026-04-30 | Sanitization extended for Pathway 1: AlphaFold AF IDs, TDC-leaked InChIKeys, Enamine catalogue SMILES are sha256-prefix-hashed in evidence; never echoed verbatim. | Same principle as `detect_clinical_overclaim`: storing raw sensitive identifier in audit creates a leak. Investigators rerun the detector locally to recover the source. | Irreversible (security invariant). | falsifiers/detectors.py — alphafold/benchmark/ipchemspace detectors. |
| D-024 | 2026-04-30 | KG schema extended: NodeType adds Target/Hit/Lead/GenerativeProposal/Disease/BindingPocket; EdgeType adds ENCODES_TARGET/HAS_DISEASE_ASSOCIATION/HAS_BINDING_POCKET. | Pathway 1 entities are conceptually distinct from cardiac mechanism graph; reusing Gene/Channel would force semantic overload and weaken K1-K3. | Reversible. | kg/schema.py. |
| D-025 | 2026-04-30 | P1.Optimize backend is `cpu_lite`, NOT `stub`. | BoTorch + Ax + REINVENT 4 RL loop are pure-Python CPU computations. Real cutover keeps `cpu_lite` (no GPU adapter swap needed). | Reversible. | pathway1/layers/optimize/adapter.py. |
| D-026 | 2026-04-30 | AiiDA NOT integrated; existing 12-table JSONL audit + hash chain remains primary. | Existing audit is swap-ready. Adding AiiDA in parallel today creates two sources of truth. Document AiiDA as future swap when GPU workloads need persistent DAG provenance. | Reversible (could add AiiDA backend to AuditWriter). | docs/CONVENTIONS.md (deferred-decisions section). |
| D-027 | 2026-04-30 | Pathway 1 falsifier registry adds 13 new R&D-specific classes; 9 of 16 existing classes apply unchanged; 7 do not apply. | Each new class is grounded in a specific R&D failure mode (PAINS, AF-D leakage, benchmark leakage, etc.) with deterministic detector logic and negative fixtures. Reuse maximizes discipline; new classes minimize false reuse. | Reversible (additions only). | falsifiers/registry.py + detectors.py. |
| D-028 | 2026-04-30 | Pathway 1 is **non-governing** for Runpod readiness; cardiac authority is the gate. P1 cutover claims rejected by `runpod-precheck` until cardiac gates pass. | Pathway 1 outputs feed the cardiac wedge; if cardiac is defective, Pathway 1's apparent passes mask the defect. Authority must be earned at the back-end before front-end claims count. Operator brief 2026-04-30 explicit: "Mark Pathway 1 as non-governing until cardiac authority gate passes. Do not delete it; quarantine its claims from Runpod readiness." | Reversible: lift quarantine when seven cardiac gates pass (see `docs/RUNPOD_READINESS.md`). | `cli.py:runpod-precheck`, `docs/RUNPOD_READINESS.md`, `docs/CONVENTIONS.md` §16. |
| D-029 | 2026-04-30 | "Safer" language FORBIDDEN in any `multi_current_balance_score` sign convention discussion (code, docs, tests). | Two prior conventions clashed (one called positive=safer, the other called positive=APD-prolongation). The "safer" framing also conflates research indicator with clinical safety claim. Single canonical convention: HIGHER = more APD-prolongation tendency in research-only models. | Reversible. | `cardiac_bridge.py`, `contracts/{l1,l5}.py`, `layers/l1/adapter.py`, `packets/assembler.py`, `docs/CONVENTIONS.md` §16. |
| D-030 | 2026-04-30 | `hERG_only_overreach` detector strengthened to inspect claim text and ranking semantics (Phase B.1), not just the panel-membership/explicit-absence pattern. | Companion-current presence in the panel was insufficient; a claim asserting hERG-only-driven proarrhythmia in prose, or a ranking that puts hERG-only above hERG+ICaL, must FAIL the detector even when companion currents exist. Brief 2026-04-30: "Strengthen hERG-only detection to inspect claim/ranking semantics, not only companion gene presence." | Reversible (default args preserve panel-only behavior). | `falsifiers/detectors.py:detect_herg_only_overreach`. |
| D-031 | 2026-04-30 | `FalsifierLedger` is populated alongside `audit/falsifiers.jsonl` and reconciled (Phase B.2). Reconciliation runs at the end of every cardiac and Pathway 1 run; failure raises `ReconciliationError`. | The ledger was instantiated but never written; audit/ledger/KG were three views of the same truth that could silently diverge. Reconciliation is a hard runtime gate. | Reversible. | `falsifiers/ledger.py`, `audit/reconciliation.py`, `runs/cardiac_run.py`, `runs/pathway1_run.py`. |
| D-032 | 2026-04-30 | Cardiac packet assembler consumes the validated L1 channel-panel envelope output (Phase C.1) — NOT `fixture["channel_panel_canned"]`. Production runs set `require_envelope=True`. | Fixtures are inputs, not authority. Reading the fixture's canned panel directly let a desynchronized envelope go undetected. Compound metadata (name/inchikey/SMILES) still seeds inputs. | Reversible: legacy fixture path remains for unit-test harness only. | `packets/assembler.py`, `runs/cardiac_run.py`, `runs/pathway1_run.py`, `layers/l1/adapter.py`, `contracts/l1.py`. |
| D-033 | 2026-04-30 | `zer0pa-biomolecular-explorer run-cardiac` is governed by the L6 router (Phase C.2). Falsifier FAIL produces a router BLOCK that prevents packet export; CLI exits 2 on block. | The falsification engine's deciding authority is the L6 router; without it, FAIL was visible only as data, not as gate. Now packet export is conditional on `report.block_count == 0` and an L6 OutputEnvelope is recorded for K4. | Reversible. | `runs/cardiac_run.py:_run_l6_governance`, `cli.py:run_cardiac`. |
| D-034 | 2026-04-30 | New `NodeType.AUDIT_RECORD` introduced and `KGValidator` enforces K1-K5 (Phase D.3). K2 codec-not-mechanism, K4 layer-coverage L1-L6, K5 episode-no-evidence are all enforced. | Reusing `OUTPUT_ENVELOPE` as an audit pointer was a temporary hack; K1 now matches on type. K2-K5 were documented but unenforced; cardiac runs invoke `validate_cardiac()` to enforce all five. | Reversible. | `kg/schema.py`, `kg/validator.py`, `runs/cardiac_run.py`. |
| D-035 | 2026-04-30 | Locked morphology fixtures (`fixtures/morphology/*.json`) replace synthetic ad-hoc QT arrays (Phase D.2). NaN/inf in any fiducial array hard-stops via `MorphologyFixtureError`. | Synthetic arrays could not be replayed; extractor provenance was missing. Now every cardiac compound has a locked fixture with extractor + version + reference table + provenance, and the loader enforces finiteness. | Reversible: fixture revisions are explicit. | `fixtures/morphology/*.json`, `packets/morphology_fixtures.py`, `runs/cardiac_run.py`. |
| D-036 | 2026-04-30 | Pre-registered, source-grounded PubMed baseline fixtures (`fixtures/pubmed_baseline/*.json`) replace the constant 49.0 baseline (Phase D.1). Held-out subset (moxifloxacin, diltiazem, mexiletine, lidocaine) reserved for blind evaluation; `BaselineHarness.evaluate_with_holdout` reports seed and held-out lift separately. Pathway 1 candidates have no calibrated baseline (D-028 quarantine). | A constant baseline is not a baseline. Per-compound calibration with rationale + source_refs is auditable; held-out compounds isolate calibration from blind evaluation. | Reversible: fixture revisions explicit; calibration changes require a DECISIONS row. | `fixtures/pubmed_baseline/*.json`, `packets/pubmed_baseline.py`, `runs/pathway1_run.py`. |
| D-037 | 2026-04-30 | `runpod-precheck` returns non-zero on real blockers and structural defects (Phase E.2). Exit 3 on adapter blockers (e.g., backend=runpod_gpu but endpoint=null), exit 4 on missing acceptance gates / parked-work / pre_cutover_state / parked-fixture-files-on-disk. | Previous behavior always returned 0; CI saw no signal. Real blockers must produce a non-zero CI exit. Parked-work entries must declare id/contract/fixture/runpod_steps/acceptance_gate, and the fixture file must exist on disk. | Reversible. | `cli.py:_runpod_precheck_logic`, `runpod.config.yaml`, `fixtures/canned/{l1,l2,reasoner}/*.json`. |
| D-038 | 2026-04-30 | Plug-swap hardened tests (Phase E.1) verify call signatures, audit provenance shape (sha256:<64-hex>), nested schema compatibility (NoneType compatible with concrete scalars; empty list compatible with populated list), and falsifier-class preservation between Stub and Toy adapters. | The previous tests verified only top-level envelope keys. Plug-replaceability discipline requires the adapter signatures and falsifier semantics to match too. | Reversible: tests are pure additions. | `tests/plug_swap/test_plug_swap_hardening.py`. |
